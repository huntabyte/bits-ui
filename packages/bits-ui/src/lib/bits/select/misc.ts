export const viewportStyles = `
<style>
	/* Hide scrollbars cross browser and enable momentum scroll for touch devices */
	[data-bits-select-viewport] { scrollbar-width:none; -ms-overflow-style: none; -webkit-overflow-scrolling: touch; }
	[data-bits-select-viewport]::-webkit-scrollbar { display: none; }
</style>
`;
