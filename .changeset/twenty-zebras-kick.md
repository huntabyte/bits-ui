---
"bits-ui": patch
---

Improve runtime performance with optimized internal `mergeProps` and `boxWith` implementations, less reactive work in Select/Tooltip/Menu, and smarter body scroll locking when a dialog overlay handles pointer blocking.
