---
"bits-ui": minor
---

feat(Slider): introduce the `thumbItems` snippet prop on `Slider.Root` as a replacement for the now-deprecated `thumbs`, enabling a more flexible API that better supports rendering `ThumbLabels`. Prefer `thumbItems` going forward.