---
"bits-ui": minor
---

feat(Slider): introduce the `tickItems` snippet prop on `Slider.Root` as a replacement for the now-deprecated `ticks`, enabling a more flexible API that better supports rendering `TickLabels`. Prefer `tickItems` going forward.
